import styled from 'styled-components'
import tw from 'twin.macro'

type Props = {}

export const Channel: React.FC<Props> = () => {
  return <Container>Channel</Container>
}

const Container = styled.div`
  ${tw`flex-1 bg-gray-500`}
`
