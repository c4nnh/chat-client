import styled from 'styled-components'
import tw from 'twin.macro'

type Props = {}

export const MessageList: React.FC<Props> = () => {
  return <Container>MessageList</Container>
}

const Container = styled.div`
  ${tw`flex-1`}
`
